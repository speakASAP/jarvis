interface:
  display_name: "LWC Tutor"
  short_description: "Durable teacher-led learning sessions"
  default_prompt: "Use $using-tutor as a silent durable control plane while responding as a natural, objective teacher."
